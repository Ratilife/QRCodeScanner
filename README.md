# QRCodeScanner

Android-приложение для сканирования QR-кодов и штрихкодов в реальном времени с использованием камеры устройства.

## 📱 Описание

QRCodeScanner — это простое и удобное Android-приложение для сканирования QR-кодов и штрихкодов. Приложение использует камеру устройства для распознавания кодов в реальном времени и отображает результат в текстовом поле.

## ✨ Функциональность

- 🔍 **Сканирование в реальном времени** — автоматическое распознавание QR-кодов и штрихкодов через камеру
- 📷 **Использование задней камеры** — автоматический выбор задней камеры устройства
- 🎯 **Мгновенное распознавание** — быстрое обнаружение и декодирование кодов
- 📝 **Отображение результата** — автоматический вывод отсканированного текста в текстовое поле
- 🔒 **Обработка разрешений** — корректная работа с разрешениями на доступ к камере
- 🛡️ **Обработка ошибок** — надежная обработка ошибок и исключений

## 🛠️ Технологии

### Основные библиотеки

- **CameraX** (1.1.0-beta02) — работа с камерой устройства
  - `camera-core` — базовые функции камеры
  - `camera-camera2` — интеграция с Camera2 API
  - `camera-lifecycle` — управление жизненным циклом
  - `camera-view` — отображение превью камеры
  - `camera-extensions` — расширенные функции

- **ML Kit Barcode Scanning** (18.3.1) — распознавание штрихкодов
  - Поддержка различных форматов штрихкодов
  - Высокая точность распознавания
  - Работа в режиме реального времени

- **AndroidX Libraries**
  - AppCompat — поддержка старых версий Android
  - Material Design — современный дизайн интерфейса
  - Activity — управление активностями
  - ConstraintLayout — гибкая компоновка интерфейса

### Языки и инструменты

- **Java** — язык программирования
- **Gradle** — система сборки
- **Android SDK** — платформа разработки

## 📋 Требования

### Системные требования

- **Минимальная версия Android**: 7.0 (API 24)
- **Целевая версия Android**: 14.0 (API 34)
- **Компиляция**: Android SDK 34
- **Java**: версия 1.8 или выше

### Разрешения

- `CAMERA` — доступ к камере устройства (обязательно)

### Оборудование

- Устройство с камерой
- Задняя камера (основная камера устройства)

## 🚀 Установка

### Предварительные требования

1. **Android Studio** — последняя версия
2. **Android SDK** — установлен и настроен
3. **Java JDK** — версия 8 или выше
4. **Устройство Android** или эмулятор с камерой

### Шаги установки

1. **Клонирование репозитория**
   ```bash
   git clone <repository-url>
   cd QRCodeScanner
   ```

2. **Открытие проекта в Android Studio**
   - Запустите Android Studio
   - Выберите `File` → `Open`
   - Выберите папку проекта `QRCodeScanner`

3. **Синхронизация проекта**
   - Android Studio автоматически начнет синхронизацию Gradle
   - Дождитесь завершения загрузки зависимостей

4. **Сборка проекта**
   - Выберите `Build` → `Make Project`
   - Убедитесь, что сборка прошла успешно

5. **Запуск приложения**
   - Подключите Android-устройство или запустите эмулятор
   - Нажмите `Run` → `Run 'app'`
   - Выберите целевое устройство

## 📖 Использование

### Первый запуск

1. **Разрешение на доступ к камере**
   - При первом запуске приложение запросит разрешение на доступ к камере
   - Нажмите `Разрешить` для продолжения работы

2. **Сканирование QR-кода**
   - Наведите камеру на QR-код или штрихкод
   - Приложение автоматически распознает код
   - Результат отобразится в текстовом поле вверху экрана

### Работа с приложением

- **Автоматическое сканирование** — приложение непрерывно анализирует изображение с камеры
- **Мгновенный результат** — отсканированный текст сразу отображается в текстовом поле
- **Обработка нескольких кодов** — при обнаружении нескольких кодов обрабатывается первый найденный

## 📁 Структура проекта

```
QRCodeScanner/
├── app/
│   ├── src/
│   │   ├── main/
│   │   │   ├── java/
│   │   │   │   └── com/
│   │   │   │       └── ratilife/
│   │   │   │           └── qrcodescanner/
│   │   │   │               └── MainActivity.java    # Основная активность
│   │   │   ├── res/
│   │   │   │   ├── layout/
│   │   │   │   │   └── activity_main.xml           # Макет главного экрана
│   │   │   │   ├── drawable/
│   │   │   │   │   └── border_frame.xml            # Рамка для сканирования
│   │   │   │   ├── values/
│   │   │   │   │   ├── strings.xml                 # Строковые ресурсы
│   │   │   │   │   ├── colors.xml                  # Цвета
│   │   │   │   │   └── themes.xml                  # Темы
│   │   │   │   └── xml/
│   │   │   │       ├── backup_rules.xml            # Правила резервного копирования
│   │   │   │       └── data_extraction_rules.xml   # Правила извлечения данных
│   │   │   └── AndroidManifest.xml                 # Манифест приложения
│   │   ├── androidTest/                            # Инструментальные тесты
│   │   └── test/                                   # Unit тесты
│   └── build.gradle.kts                           # Конфигурация сборки модуля
├── gradle/
│   └── libs.versions.toml                         # Версии зависимостей
├── build.gradle.kts                               # Конфигурация сборки проекта
├── settings.gradle.kts                            # Настройки проекта
└── README.md                                      # Документация проекта
```

## 🔧 Настройка и конфигурация

### Изменение разрешения сканирования

В файле `MainActivity.java` можно изменить разрешение сканирования:

```java
ImageAnalysis imageAnalysis = new ImageAnalysis.Builder()
    .setTargetResolution(new Size(1280, 720))  // Измените разрешение здесь
    .setBackpressureStrategy(ImageAnalysis.STRATEGY_KEEP_ONLY_LATEST)
    .build();
```

### Изменение стратегии обработки кадров

Текущая стратегия: `STRATEGY_KEEP_ONLY_LATEST` — обрабатывается только последний кадр.

Альтернативные стратегии:
- `STRATEGY_BLOCK_PRODUCER` — блокирует получение новых кадров до обработки текущего

## 🐛 Известные проблемы

- Приложение использует beta-версию CameraX (1.1.0-beta02) — рекомендуется обновить до стабильной версии
- Нет защиты от повторного сканирования одного и того же QR-кода
- Отсутствует история отсканированных кодов
- Нет возможности копировать результат сканирования

## 🔮 Планы на будущее

- [ ] Добавить историю отсканированных кодов
- [ ] Добавить возможность копирования результата
- [ ] Добавить звуковой/вибро-сигнал при успешном сканировании
- [ ] Улучшить UI с визуальной рамкой для сканирования
- [ ] Добавить поддержку экспорта истории
- [ ] Обновить CameraX до стабильной версии
- [ ] Добавить поддержку фронтальной камеры
- [ ] Добавить настройки приложения

## 🤝 Вклад в проект

Если вы хотите внести вклад в проект:

1. Создайте форк проекта
2. Создайте ветку для новой функции (`git checkout -b feature/AmazingFeature`)
3. Зафиксируйте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Отправьте изменения в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📝 Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для получения дополнительной информации.

## 👤 Автор

**RatiLife**

- Проект: [QRCodeScanner](https://github.com/ratilife/QRCodeScanner)

## 🙏 Благодарности

- [CameraX](https://developer.android.com/training/camerax) — библиотека для работы с камерой
- [ML Kit](https://developers.google.com/ml-kit) — библиотека машинного обучения от Google
- [Android Developers](https://developer.android.com/) — документация и ресурсы для разработчиков Android

## 📞 Контакты

Если у вас есть вопросы или предложения, создайте [Issue](https://github.com/ratilife/QRCodeScanner/issues) в репозитории проекта.

---

**Примечание**: Это приложение предназначено для образовательных целей и демонстрации возможностей сканирования QR-кодов на Android.

